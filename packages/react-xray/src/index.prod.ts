/**
 * Production stub — XRay renders nothing in production builds.
 * Zero runtime cost, zero bundle size impact.
 */
const XRay = () => null
export default XRay

export type { XRayAllInOneProps, EditorPreset } from './index'
